apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-config-map
  namespace: vault
data:
  NAMESPACE: vault
  VAULT_ADDR: http://vault-0.vault-internal:8200
  VAULT_SKIP_VERIFY: "true"
  NUM_OF_NODES: 3
